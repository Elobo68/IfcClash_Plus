# Description

This rule is the same as Above Rule but with below.

The two rules are needed, when one rule could be enough (If A is on above of B, then B is below A).
I consider that rule have a direction.
- A VS B will produce a list of A objects.
- B VS A will produce a list of B objects.
The two results are not identical.



# Property

# Result


# Example

